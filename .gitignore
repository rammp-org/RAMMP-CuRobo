__pycache__/
*.py[cod]
*.egg-info/
build/
install/
log/
dist/
.pytest_cache/
.ruff_cache/
*.npy
*.npz
*.MOV

# AnyGrasp licenses are per-machine credentials — never publish them
# (the vendor SDK gitignores them too). Keep the files, not the history.
docs/license_*/
*.lic
*.signature
*.public_key

# generated fiducials (scripts/make_tag.py)
tag*.png

# raw hand-eye sweeps (re-solvable data, not source)
*_views.npz
