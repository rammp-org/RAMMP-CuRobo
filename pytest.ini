[pytest]
; -p no:anyio: the Jetson's user site carries an anyio pytest plugin built
; for a newer pytest than Ubuntu 22.04 ships — autoloading it crashes
; collection (ModuleNotFoundError: _pytest.scope).
addopts = -p no:anyio
testpaths = core/tests
